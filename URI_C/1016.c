#include <stdio.h>

int main()
{
    //Variaveis 
    int distancia, tempo;
    //Entrada de dados 
    scanf("%d", &distancia);
    //Processamento
    tempo = distancia * 2;
    //Saída de dados
    printf("%d minutos\n", tempo);
    
    return 0;
}